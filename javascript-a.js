function largest() {
    var big = Number(document.getElementById("frist").value);
    var small = Number(document.getElementById("second").value);

    if (big > small) {
        window.alert(big + "-is Larger");
    }
    else {
        window.alert(small + "-is larger");
    }
}




























